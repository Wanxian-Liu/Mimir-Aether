# Parity snapshots (H15)

JSON files here are **historical captures** from `scripts/diff_tool_names_hermes_mimir.py --json`. The `_meta.hermes_root` / `_meta.mimir_root` fields use **placeholder paths** (`/path/to/...-clone`) so snapshots stay machine-independent; re-run the script locally to refresh with your real clone paths.
