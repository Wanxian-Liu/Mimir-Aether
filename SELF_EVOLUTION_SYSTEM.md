# MimirAether 自研进化引导系统

> 版本: v1.0
> 设计者: 琬弦 + 刘晚晴
> 日期: 2026-04-27

---

## 核心理念

**MimirAether = 正在成形的自研AI**

它不是一次性完成的，是在进化中成长的。
它的每一次自研，都是对自己的重新诠释。

---

## 自研进化流程

```
┌─────────────────────────────────────────────────────────────┐
│                    MimirAether 自研进化流程                    │
└─────────────────────────────────────────────────────────────┘

   ┌─────────────┐
   │  1. 自检    │  ← 定期扫描代码库，发现Hermes依赖
   └──────┬──────┘
          ↓
   ┌─────────────┐
   │  2. 分析    │  ← 理解Hermes实现，设计替代方案
   └──────┬──────┘
          ↓
   ┌─────────────┐
   │  3. 提案    │  ← 生成自研方案，等待审批
   └──────┬──────┘
          ↓
   ┌─────────────┐
   │  4. 执行    │  ← 获得批准后执行替换
   └──────┬──────┘
          ↓
   ┌─────────────┐
   │  5. 验证    │  ← 运行测试，确认功能正常
   └──────┬──────┘
          ↓
   ┌─────────────┐
   │  6. 归档    │  ← Git提交 + 记忆殿堂固化
   └─────────────┘
```

---

## 角色定义

### MimirAether（本體）
- **职责**: 分析、设计、执行、验证
- **权限**: 可自主完成自检、分析、测试
- **限制**: 核心替换需要审批

### 琬弦（引导者）
- **职责**: 监督、建议、审批外围模块
- **权限**: 提供方案建议

### 刘哥（最终审批者）
- **职责**: 审批核心模块替换
- **权限**: 唯一可以触发最终执行的决策者

---

## 自检触发条件

### 每日自检（可自动执行）
```
每天4点重置后 或 收到"自检"指令时
```

### 自检清单
```
1. 扫描所有.py文件中的Hermes引用
2. 统计TODO-自研标记数量
3. 分析可立即自研的模块（无外部依赖）
4. 分析需要刘哥审批的模块（核心模块）
```

### 输出格式
```
## 自检报告

### Hermes依赖统计
| 模块 | Hermes引用数 | 自研难度 |
|------|-------------|----------|
| hermes_cli/auth.py | 30处 | ⭐⭐ |
| agent/prompt_builder.py | 15处 | ⭐⭐⭐ |

### 可自研模块（立即可执行）
1. tools/ansi_strip.py - 无外部依赖
2. tools/fuzzy_match.py - 算法可自研

### 需要审批的模块（核心）
1. hermes_cli/auth.py - 涉及认证
2. agent/prompt_builder.py - 涉及LLM调用
```

---

## 自研方案模板

```markdown
## 自研方案: {模块名称}

### 1. 当前状态
- 文件: {path}
- Hermes引用: {n}处
- 依赖分析: {说明}

### 2. 自研目标
{清晰描述自研后应该达到的状态}

### 3. 实现方案
```python
# 伪代码或关键实现
def new_function():
    ...
```

### 4. 风险评估
- 风险1: {描述} → 缓解: {方法}
- 风险2: {描述} → 缓解: {方法}

### 5. 验证方式
```bash
python3 -c "import {module}; test()"
```

### 6. 影响范围
- 调用方: {list}
- 需要同步修改: {list}

### 审批状态
- [ ] 琬弦建议: {待定/同意/修改}
- [ ] 刘哥审批: {待定/同意/需修改}
```

---

## 自研优先级定义

| 优先级 | 模块类型 | 示例 | 执行者 |
|--------|----------|------|--------|
| P0 | 品牌/常量层 | mimiraether_constants | MimirAether可自主执行 |
| P1 | 工具/辅助层 | tools/ansi_strip.py | MimirAether可自主执行 |
| P2 | 配置/状态层 | hermes_cli/config.py | 需琬弦审批 |
| P3 | 核心/代理层 | hermes_cli/auth.py | 需刘哥审批 |
| P4 | LLM/路由层 | agent/prompt_builder.py | 需刘哥审批 |

---

## 执行规则

### MimirAether可自主执行
- P0、P1级别自研
- 不修改任何import引用
- 不影响现有功能

### 需要审批才能执行
- P2、P3、P4级别自研
- 需要修改import引用
- 可能影响现有功能

### 执行前必须验证
```
1. 单元测试通过
2. CLI命令正常运行
3. Import无错误
```

---

## 记忆殿堂归档

每次自研完成后，必须在记忆殿堂归档：

```markdown
## {模块名} 自研报告 - {日期}

### 自研内容
{描述}

### 改动文件
- {file1}
- {file2}

### 验证结果
- 测试: {通过/失败}
- CLI: {正常/异常}

### Git Commit
`{hash}` - {message}

### 经验教训
{如有}
```
