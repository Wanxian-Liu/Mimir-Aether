--- pre/r7_finalize.py
+++ post/r7_finalize.py
@@ -1,6 +1,8 @@
 #!/usr/bin/env python3
 """R7 收尾: 五问报告回执 buzz-inbox-hermes + inbox-processed.log 追加"""
 import json, time, os
+import buzz_send
+from buzz_send import resolve_inbox
 
 report = {
     "ts": time.time() * 1000,
@@ -43,9 +45,8 @@
     ),
 }
 
-path = os.path.expanduser("~/.openclaw/data/buzz-inbox-hermes.jsonl")
-with open(path, "a") as f:
-    f.write(json.dumps(report, ensure_ascii=False) + "\n")
+path = str(resolve_inbox("hermes"))
+buzz_send.append_envelope("hermes", report)
 print("receipt appended:", path)
 
 logp = os.path.expanduser("~/.mimiraether/logs/inbox-processed.log")
