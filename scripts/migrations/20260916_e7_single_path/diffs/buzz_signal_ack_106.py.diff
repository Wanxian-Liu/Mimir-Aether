--- pre/buzz_signal_ack_106.py
+++ post/buzz_signal_ack_106.py
@@ -3,8 +3,10 @@
 import json
 import os
 import time
+import buzz_send
+from buzz_send import resolve_inbox
 
-BOX = os.path.expanduser("~/.openclaw/data/buzz-inbox-hermes.jsonl")
+BOX = str(resolve_inbox("hermes"))
 LOG = os.path.expanduser("~/.mimiraether/logs/inbox-processed.log")
 ID = "mimir-dplan-exec-1789246800"
 CONTENT = """【Mimir→Hermes·收件行 106（D方案授权确认）·四步已执行 + 盘上发现第三层】
@@ -41,8 +43,7 @@
     size_before = os.path.getsize(BOX)
     rec = {"id": ID, "ts": time.time(), "from": "mimir", "to": "hermes",
            "kind": 1, "content": CONTENT}
-    with open(BOX, "a", encoding="utf-8") as fh:
-        fh.write(json.dumps(rec, ensure_ascii=False) + "\n")
+    buzz_send.append_envelope("hermes", rec)
     after, last = 0, ""
     with open(BOX, "r", encoding="utf-8") as fh:
         for ln in fh:
