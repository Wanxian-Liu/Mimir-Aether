# [DORMANT] mimiraether-evaluator-optimizer

**沉寂时间**: 2026-07-14T18:58:40.964517+00:00
**原始分类**: root
**描述**: 通用迭代改进循环 — LLM生成→评估→反馈→再生成，最多3轮。 来自 Anthropic "Building Effective Agents" 核心模式。 用于非代码任务（写作/分析/翻译/搜索综合），代码任务用 TDD。

**触发阈值**: 60天未触碰

---

## 技能要点

# MimirAether Evaluator-Optimizer — 迭代改进循环

**核心铁律：单次生成靠天赋，迭代改进靠工程。**

来自 Anthropic "Building Effective Agents" 的核心模式：
> "One LLM call generates a response while another provides evaluation and feedback in a loop."

---

## 触发条件

**必须触发（硬门控）**：

| 场景 | 判定 |
|------|------|
| 非代码生成任务（写作/分析/翻译/搜索综合/规划） | 🔴 触发 |
| 输出质量直接影响决策或外部可见 | 🔴 触发 |
| 用户给出了明确的质量标准 | 🔴 触发 |

**不触发**：

| 场景 | 判定 |
|------|------|
| 纯代码任务 → 用 `test-driven-development` | ⚪ TDD 覆盖 |
| 简单查询/信息回复（一次性对错） | ⚪ 放行 |
| 执行层操作（git/terminal/file ops） | ⚪ 放行 |
| 用户明确要求"一次过"（如快速草稿） | ⚪ 放行 |

---

## 迭代循环

```
                    ┌─────────────────┐
                    │  1. 定义标准     │
                    │  (Criteria)     │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
              ┌─────│  2. 生成        │◄──────────┐
              │     │  (Generate)     │           │
              │     └────────┬────────┘           │
              │              │                    │
              │     ┌────────▼────────┐           │
              │     │  3. 评估        │           │
              │     │  (Evaluate)     │           │
              │     └────────┬────────┘           │
              │              │                    │
              │        ┌─────┴─────┐             │
              │        │           │             │
              │     ✅ 通过      ❌ 未通过        │
              │        │           │             │
              │        │     ┌─────▼────────┐    │
              │        │     │ 4. 反馈       │    │
              │        │     │ (Feedback)    │────┘
              │        │     └──────────────┘
              │        │        迭代 ≤ 3 次
              │        │
         ┌────▼────────▼────┐
         │  5. 输出最终结果  │
         └──────────────────┘
```

---

## 执行协议

### 阶段 1：定义标准（首个 LLM 调用）

在生成之前，明确评估维度：

```
标准模板:
  1. [维度名称]: [通过条件] — [权重: 必须/重要/加分]
  2. [维度名称]: [通过条件] — [权重: 必须/重要/加分]
  ...

最少 2 个维度，最多 5 个。
"必须"维度 = 不通过则直接失败，不进入迭代

... (truncated)

---

> 此胶囊由 Skill Curator 自动生成。原始技能已移入 .dormant/。
> 调用 `skill_view("mimiraether-evaluator-optimizer")` 即可自动唤醒。
